targetPort: 5000 # port of pod container
ingress:
  enable: true
  host: uat-api-insurance.baohiemtasco.vn
  path: /acg-xm
#  annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /$1
envFrom:
  configMaps:
    - name: common-cm
    - name: redis1-cm
    - name: keycloak-cm

# hpa:
#   enable: true
#   minReplicas: 1
#   maxReplicas: 10
#   cpuUtilization: 80

depCommand: ./run.sh